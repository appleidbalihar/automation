import type { ReactElement } from "react";
import { WorkflowBuilderPanel } from "../../workflow-builder";

export default function WorkflowsPage(): ReactElement {
  return <WorkflowBuilderPanel />;
}
