# Progress Tracker

| Date | Milestone | Status | Notes |
| --- | --- | --- | --- |
| 2026-04-08 | Workspace foundation | In Progress | Bootstrapping microservice monorepo and tracking artifacts. |
| 2026-04-08 | Core implementation slice | In Progress | Added shared packages, Prisma schema, microservice shells, checkpoint-aware order execution path, and enterprise web shell. |
| 2026-04-09 | Runtime bootstrap and smoke validation | Completed | Docker Compose stack started, Prisma migration applied, service health checks passed, and workflow->publish->order execute smoke flow succeeded. |
| 2026-04-09 | API gateway forwarding | Completed | Replaced gateway stubs with live forwarding to workflow/order/logging/chat services, including query/body/header propagation and upstream error handling. |
| 2026-04-09 | Gateway RAG + dependency health | Completed | Added gateway forwarding for rag search/index and a dependency health endpoint with per-service status and 503 handling when any downstream service is unhealthy. |
| 2026-04-09 | Integration adapter implementation | Completed | Replaced integration stub with real adapter engine for REST/SSH/NETCONF/SCRIPT, added adapter tests, and validated live SCRIPT + REST execution paths. |
| 2026-04-10 | Integration security hardening | Completed | Added env-based credential references (`env:VAR`), secret masking in outputs/errors, dangerous-command blocking, and configurable allowlist policy for REST/SSH/NETCONF/SCRIPT adapters. |
| 2026-04-10 | Integration policy defaults + secret provider abstraction | Completed | Added secret resolver abstraction (current provider `env`), strict production-default allowlists (deny by default for non-REST), and tests validating production policy enforcement. |
| 2026-04-10 | Vault reference support + integration audit logging | Completed | Added `vault:path#field` secret resolution support, fixed colon-string resolution to only `env:`/`vault:` prefixes, and emitted integration execution audits to logging-service when metadata includes order correlation fields. |
| 2026-04-10 | Vault smoke reliability + logging timeline APIs | Completed | Added readiness-wait smoke scripts for Vault/integration-service, added `/logs/timeline` API, expanded `/logs` filters (including correlation ID), and strengthened recursive payload masking in logging-service. |
| 2026-04-10 | RabbitMQ execution event pipeline (v1) | Completed | Added `order-service` domain-event publishing to `platform.events` and `logging-service` durable consumer ingestion (`source=event-bus`) with masked payload persistence and ack/nack handling. |
| 2026-04-10 | Workflow/RAG event publishing + delivery tests | Completed | Added `workflow.published` and `rag.index.requested` RabbitMQ publishers with correlation support and added publisher delivery tests validating routing key, envelope shape, and persistent JSON publishing. |
| 2026-04-10 | Workflow + RAG event workers with durable state | Completed | Added dedicated consumers for `workflow.published` and `rag.index.requested`, persisted worker state in `WorkflowPublishAudit` and `RagIndexJob`, added worker status endpoints, and applied DB migration for restart-safe tracking. |
| 2026-04-10 | Real RAG indexing + gateway e2e smoke | Completed | Added `RagDocument` persistence, implemented worker-driven indexing from source docs, switched `/rag/search` to indexed DB retrieval, and added `smoke:rag` for gateway index->job->search validation. |
| 2026-04-10 | UI operations panels + recovery timeline smoke | Completed | Added live dashboard panels for workflow publish audits and RAG jobs, and added `smoke:recovery` validating execute->rollback->retry timeline transitions via gateway/logging APIs. |
| 2026-04-10 | Keycloak JWT auth hardening (gateway) | Completed | Added Keycloak JWT verification via JWKS in `@platform/auth`, mapped realm/client roles to RBAC roles, kept legacy local bearer parsing for compatibility, and added auth unit tests. |
| 2026-04-10 | Role-aware UI action controls + recovery smoke resilience | Completed | Added web action controls for publish/index/execute/retry/rollback with role-based enablement and status feedback; hardened `smoke:recovery` to reuse healthy running services and avoid port-collision failures. |
| 2026-04-10 | Token-driven UI identity + strict legacy bearer policy | Completed | Shifted web ops panel auth to token storage + `/auth/me`, disabled legacy non-JWT bearer parsing by default, and required explicit `AUTH_ALLOW_LEGACY_BEARER=true` for smoke/dev compatibility. |
| 2026-04-10 | Workflow builder interactive UI (v1) | Completed | Replaced workflow-builder placeholder with interactive node/step draft editor, save-as-new workflow action, publish-selected action, and version awareness table wired to gateway workflow APIs. |
| 2026-04-10 | Order execution console + approval queue UI (v1) | Completed | Added tracked-order execution console with status/checkpoint/failure visibility, timeline retrieval, retry/rollback actions, and approval queue view derived from tracked `PENDING_APPROVAL` orders. |
| 2026-04-10 | RBAC + resume e2e smoke coverage | Completed | Added `smoke:rbac` flow validating viewer/operator/admin gateway authorization boundaries plus retry `resumeFrom` pointer consistency with persisted order checkpoint state and timeline transitions. |
| 2026-04-10 | Order list + approval list backend APIs | Completed | Added `GET /orders` and `GET /orders/approvals` in order-service and gateway, switched web execution console approval queue to backend-driven listing, and expanded RBAC smoke to validate list endpoint access. |
| 2026-04-10 | Approval lifecycle end-to-end implementation | Completed | Added persisted approval decisions, approval request/approve/reject APIs, engine `PENDING_APPROVAL` state support, UI approval actions, and smoke coverage for approval RBAC + transition behavior. |
| 2026-04-10 | Execution-engine service implementation | Completed | Replaced execution-engine skeleton with workflow validation and checkpoint-aware run endpoints, added engine validation tests, and documented operational usage for direct engine behavior checks. |
| 2026-04-10 | Chat-service operational context hardening | Completed | Added stricter backend/code guardrails and context-aware operational answers using live order/workflow records with persisted chat history continuity. |
| 2026-04-10 | Final smoke/test polish | Completed | Added `smoke:engine` and `smoke:all` runner scripts plus chat-service guardrail/context unit tests to strengthen production-readiness validation coverage. |
| 2026-04-10 | Full containerized microservice runtime | Completed | Containerized all app services, added db migration container, fixed Prisma runtime compatibility on Debian images, and verified web/API/dependency health via Docker Compose. |
| 2026-04-10 | Production web routing baseline | Completed | Replaced single-page simulated nav with real URL routes (`/dashboard`, `/workflows`, `/orders`, `/approvals`, `/logs`, `/assistant`), added live dashboard API data, and verified route availability on running containers. |
| 2026-04-10 | Real execution runtime chain and recovery hardening | Completed | Replaced simulated order step execution with service-chain runtime (`order-service -> execution-engine -> integration-service`), persisted step request/response payloads + timestamps, implemented immediate retry execution from checkpoint, and added rollback-action execution over completed reversible steps. |
| 2026-04-11 | Web login + JWT production auth alignment | Completed | Added real web sign-in flow via `/api/auth/token`, introduced route-level session gate and logout UX, switched compose default to strict non-legacy bearer auth, and hardened JWT verification to accept internal/public issuer patterns with client binding checks. |
| 2026-04-11 | Node integration + per-order environment runtime wiring | Completed | Added integration profile CRUD and per-user environment CRUD in workflow-service/gateway, enabled node-level `integrationProfileId` assignment in builder UI, persisted order `environmentId` + snapshot, and fixed integration-service to resolve `env:` references from selected order environment while composing REST requests from integration base config + step variables. |
| 2026-04-11 | External Integration management page + ownership/share/LCM controls | Completed | Added dedicated `/integrations` page with tabs (`Add Node Integration`, `Environment`), scope toggles (owned/shared/all), owner-enforced integration/environment mutation, username sharing, duplicate-copy ownership transfer, integration auth-test action, and workflow-usage warnings that block integration deactivate/terminate when referenced by workflows. |
| 2026-04-11 | Admin/useradmin role split + Keycloak user LCM | Completed | Added `useradmin` role model with `operator` backward aliasing, changed self-registration default role to `useradmin`, added admin-only Keycloak user lifecycle APIs/UI (`/users`), added authenticated profile password change flow (`/profile`), and tightened non-admin ownership scope for integration/environment and order queries/actions. |
| 2026-04-11 | Postman-style integration auth UX + runtime auth flow | Completed | Replaced integration JSON auth editor with dynamic form-based auth config (`No Auth`, `Basic`, `API Key`, `OAuth2`), added explicit Base URL/method inputs, added `NO_AUTH` contract support, added URL/credential `{{envVar}}` interpolation from selected environment, and implemented OAuth2 token acquisition/cache in integration-service for auth test and runtime calls. |
| 2026-04-11 | Automated TLS runtime + Vault PKI distribution | Completed | Added shared TLS hot-reload runtime package, wired backend services for cert/key/CA reload and TLS diagnostics endpoint, enabled internal HTTPS service URLs, and added Vault persistent bootstrap + per-service Vault Agent cert rendering + automated rotation controller scaffold in Docker Compose. |
| 2026-04-11 | Secure-only cutover hardening (HTTPS/mTLS baseline) | Completed | Switched gateway ingress and web internal proxy defaults to HTTPS, enforced secure URL defaults in shared config/env templates, added TLS listener configs for PostgreSQL/Redis/RabbitMQ/Keycloak/MinIO compose services, and validated secure build + compose model with Vault-issued cert mounts. |
| 2026-04-12 | Certificate alerting + admin rotation controls | Completed | Added admin certificate health APIs/UI (`/admin/security/certificates`, `/security`), webhook-capable cert alerts, certificate event contracts, readiness TLS checks, shared rotation request queue (`/rotation-control/requests.jsonl`), and wired manual/policy triggers to the existing rotation controller workflow without introducing a new renewal owner. |
| 2026-04-12 | GitLab registry CI pipeline + offline production compose | Completed | Added `.gitlab-ci.yml` to build/push app images and mirror infra images to GitLab registry, added `docker-compose.prod.yml` that references registry images only (no local build), and added production env/documentation for internet-isolated deployments. |

## Active blockers
- End-to-end secure stack smoke (all services together) still pending after strict TLS listener cutover; next resume point includes full compose bring-up and secure smoke execution.

## Recovery note
- Resume runtime with `pnpm compose:up` from repo root.
- Validate Vault + integration secret path with `pnpm smoke:vault`.
- Validate async RAG flow with `pnpm smoke:rag`.
- Validate retry/rollback timeline with `pnpm smoke:recovery`.
- Validate RBAC + retry-resume e2e coverage with `pnpm smoke:rbac`.
- Validate execution-engine behavior with `pnpm smoke:engine`.
- Run full validation sweep with `pnpm smoke:all`.
- Verify TLS status per service using `/security/tls` with `x-security-token` when configured.
- Verify certificate control plane via `/admin/security/certificates` and `/admin/security/certificates/events`.
- Validate manual rotate path from Security page and confirm controller queue drain (`/rotation-control/requests.jsonl`).
- Validate secure endpoints only: `https://<host-ip>:443` (web), `https://<host-ip>:4000` (gateway), `https://<host-ip>:8443` (keycloak).
